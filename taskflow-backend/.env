# Django
SECRET_KEY=#aegc9i71j=@-72tw4r*urd(ls$-%k4@$i5nhm4hs7(fnftyua
DEBUG=True
DJANGO_ENV=development
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (Use local PostgreSQL for now)
DB_NAME=taskflow_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis (Use local Redis for now)
REDIS_URL=redis://localhost:6379/0

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# AWS (Leave empty for local development, add later)
AWS_ACCESS_KEY_ID=SECRET_KEY
AWS_SECRET_ACCESS_KEY= SECRET_KEY
AWS_STORAGE_BUCKET_NAME= taskflow-bucket
AWS_S3_REGION_NAME=eu-west-2